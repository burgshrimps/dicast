# Test-only dependencies for the dicast unit-test suite.
#
# Everything the test suite needs to *import* dicast at runtime (pysam,
# pandas, numpy, pyBigWig, bioframe, vcfpy, xgboost, pyyaml, tqdm, joblib,
# packaging, networkx, matplotlib) already comes from the conda environment
# (environment.yml). This file adds only the test runner.
pytest
